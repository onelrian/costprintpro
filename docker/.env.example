# CostPrint Pro Environment Configuration
# Copy this file to .env and update the values for your environment

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
POSTGRES_DB=costprint
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-database-password-here
DATABASE_URL=postgresql://postgres:your-secure-database-password-here@db:5432/costprint

# ===========================================
# REDIS CONFIGURATION (Optional)
# ===========================================
REDIS_URL=redis://redis:6379

# ===========================================
# BACKEND CONFIGURATION
# ===========================================
# JWT Secret - MUST be changed in production
JWT_SECRET=your-super-secure-jwt-secret-key-change-in-production-environment

# Server Configuration
PORT=8080
BIND_ADDRESS=0.0.0.0:8080

# CORS Origins - Add your frontend URLs
CORS_ORIGINS=http://localhost:3000,http://frontend:3000,http://127.0.0.1:3000

# Logging Level (debug, info, warn, error)
RUST_LOG=info

# ===========================================
# FRONTEND CONFIGURATION
# ===========================================
# API URL for frontend to connect to backend
NEXT_PUBLIC_API_URL=http://localhost:8080

# Node Environment
NODE_ENV=production

# Frontend Port
NEXT_PORT=3000

# Disable Next.js telemetry
NEXT_TELEMETRY_DISABLED=1

# ===========================================
# DEVELOPMENT OVERRIDES
# ===========================================
# Uncomment these for development environment

# RUST_LOG=debug
# NODE_ENV=development
# WATCHPACK_POLLING=true

# ===========================================
# PRODUCTION SECURITY SETTINGS
# ===========================================
# For production deployment, ensure you:
# 1. Change all default passwords
# 2. Use strong JWT secrets (64+ characters)
# 3. Configure proper CORS origins
# 4. Enable SSL/TLS certificates
# 5. Set up proper firewall rules
# 6. Use environment-specific secrets management

# Example strong JWT secret (generate your own):
# JWT_SECRET=your-very-long-and-secure-jwt-secret-key-with-at-least-64-characters-for-production

# Example production CORS (replace with your actual domains):
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,https://app.yourdomain.com
